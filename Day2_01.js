
const browserVersion = "chrome"

function  getBrowserVersion()
{
    if (browserversion ="chrome")
        {
            let browserVersion="chromeV6"
            console.log("call inside the block", browserVersion);
        }
        console.log("call inside the function", browserVersion);
}
getBrowserVersion() 